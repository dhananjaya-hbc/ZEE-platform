{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",

      "// OTP codes": "In Development the emailed code is written to the logs instead of",
      "// being sent": "being sent. Grab it from `docker compose logs api`.",
      "Zee.Infrastructure.Email": "Debug",

      "// EF SQL": "Set to Information to see every generated query. Useful when working",
      "// on repos": "on the repository layer; very noisy otherwise.",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },

  "// NOTE": "These defaults let `dotnet run` work against the Compose datastores with no",
  "// ": "extra setup. They are development-only values and are safe to commit.",

  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=zee;Username=zee;Password=zee",
    "Redis": "localhost:6379"
  },

  "Jwt": {
    "Key": "dev-only-insecure-signing-key-change-me-32chars",
    "Issuer": "zee"
  },

  "AiService": {
    "BaseUrl": "http://localhost:8000",
    "InternalKey": "dev-internal-key-change-me"
  }
}
